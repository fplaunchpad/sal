import Sal.ConditionedMRDTs.Framework.Base.CRDT_TS

/-!
# Forwarding stub: content moved

The CRDT transition-system core (`Configuration`, `lo`, `loOn`, `updateRep`,
`IsCanonicalState`, `SatisfiesVCs`, …) now lives in the metatheory development
tree so it can be modified there without touching the emulation framework's
layout: `Sal/MRDTs/Metatheory/Development/Emulation/CRDT_TS.lean`.
All declarations keep the `Sal.Emulation` namespace; this stub keeps every
existing `import Sal.Emulation.CRDT_TS` working unchanged.
-/
